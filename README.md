sample-TCP-IP-Server-Client
===========================

A sample Server/Client program used by TCP/IP socket.Client recive information from stdin and send to server,then server return infotmation length to Client.
